# Project Workflow Files

These files are optional downstream assets that make the KB part of normal delivery:

- `KB-PROCESS.md`: trigger matrix, ownership, labels, and definition-of-done guidance

It is not part of the strict KB file contract, but the scaffold script can install it into downstream repos.
